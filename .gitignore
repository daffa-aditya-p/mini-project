# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
# build/ - COMMENTED: We want build/ in GitHub for web/windows builds!
develop-eggs/
# dist/ - COMMENTED: We want dist/ in GitHub for executables!
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Pygame
settings.json

# PyInstaller
*.manifest
*.spec
!buildozer.spec

# Buildozer
.buildozer/
bin/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build cache - only ignore cache, not the actual builds
# build/web-cache/ - ALLOW web-cache too for consistency

# Large binaries (executables and packages > 10MB should use GitHub Releases)
build/windows/**/*.pkg
build/windows/**/*.pyz
build/windows/**/*.zip
dist/windows/ModernFlappyBird
dist/**/*.exe
